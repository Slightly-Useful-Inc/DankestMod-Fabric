package com.slightlyuseful.DankestMod.Tools;

import net.minecraft.item.AxeItem;
import net.minecraft.item.ToolMaterial;

public class CustomAxe extends AxeItem {
    public CustomAxe(ToolMaterial material, float f, float attackSpeed, Settings settings) {
        super(material, f, attackSpeed, settings);
    }
}
